{
	"extends": "../../tsconfig.json",
	"compilerOptions": {
		"lib": [
			"ESNext",
			// in order to support web workers, we need this for subtle crypto
			"DOM"
		],
		"noEmit": false,
		"outDir": "dist/esm"
	},
	"include": ["src", "tests"]
}
