{
	"extends": "@telgram-auth/tsconfig/base.json",
	"compilerOptions": {
		"lib": [
			"ESNext",
			// in order to support web workers, we need this for subtle crypto
			"DOM"
		],
		"noEmit": false,
		"declarationDir": "dist/types",
		"outDir": "dist/esm",
	},
	"include": [
		"src"
	]
}